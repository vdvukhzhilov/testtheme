import { RichText } from "@hubspot/cms-components";
import { Heading } from "../../../ui/kit/Heading";


export default function Main(props: any) {
  const { fieldValues } = props;
  const { heading, content } = fieldValues;

  console.log(content);
  return (
    
  );
}
